A CSS only loader, created using SASS.

You can change the size of the loader by changing the font-size attribute of the .css-loader selector.

@TODO:
- make number of circles configurable. This is now limited to 8. Changing this value now causes issues with positioning.